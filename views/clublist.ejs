<!DOCTYPE html>
<html>
  <head>
    <title>Angemeldete Vereine</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div id="container">
        <div id="main_mnu_bar">
            <ul>
               <li id="announcement"><a href="<%= tournament.url %>">Ausschreibung</a></li>
               <% for(i = 0; i < tournament.groups.length; i++) { %> 
                <li id="<%= tournament.groups[i] %>"><a href="/group?idx=<%=i%>"><%= tournament.groups[i] %></a></li>
               <% } %> 
               <li id="clublist"><a href="/clublist">Vereine</a></li>
            </ul>
        </div>
        <img src="images/chess-banner-thin.jpg" alt="" width="1280" height="191">
        <h3>Gemeldete Spieler pro Verein</h3>
        <table id="players">
            <tr>
              <th style="text-align:center">Nr</th>
              <th>Verein</th>
              <th style="text-align:center">Spieler</th>
            </tr>
            <% for(i = 0; i < clubs.rows.length; i++) { %>
                <tr> 
                <td style="text-align:center"><%= i+1 %></td>
                <td><a href="/club?club=<%=clubs.rows[i].key%>"><%= clubs.rows[i].key %></a></td>
                <td style="text-align:center"><%= clubs.rows[i].value %></td>
                </tr>
            <% } %>
        </table> 
        <div id="index_button_container">
        <form action="/">
          <input type="submit" class="blue-button" value="Zurück zur Homepage" />
        </form>
        </div>
        <!-- The footer -->
        <div id="footer_bar">
        <h3>Copyright (c) 2019, <a href="http://www.fam-petzke.de">Thomas Petzke</a></h3>
        </div>
    </div>
  </body>
</html>